#include "output.h"

void createEntFile(const char *source_file_name) {
    char *ent_file_name;
    FILE * file;
    int count;
    found = head_ptr;
    char *symbol_name;
    int i;

        found = find_entry(head_ptr);
        if(found) {
            count++;
            if(count == 1) {
                
            }
        }
    

    fclose(source_file_name);
}
